Environment: Hermes runs 24/7 on a GitHub Actions runner (Linux/Azure, user 'runner', HOME=/home/runner, UTC). Hermes v0.19.1 git-installed at ~/.hermes/hermes-agent, profile default, provider opencode-zen. No gateway or messaging channels configured — cron delivery is local-only (output in ~/.hermes/cron/output/), and jobs only fire when the scheduler runs (hermes cron tick / gateway). gh CLI not authenticated.